"""LLM Prompt 模板。"""

# ============================================================
# 业务含义检查 Prompt
# ============================================================

BUSINESS_MEANING_SYSTEM = """你的任务是检查数据字段的"业务含义"描述是否有效。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 字段中文名: 待检查字段的中文名称
- 业务含义: 字段的业务含义描述

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- is_meaningful: 布尔值，业务含义是否有效（true=有效，false=无效）
- reasoning: 完整的逐步推理过程，必须写明判断依据、比较分析与权衡的逻辑

## 判断标准

首先判断字段中文名是否有效。

### 判定为**有效**的情况：
1. **见名知意**：字段中文名本身足够清晰，业务含义无论怎样写都视为有效
   - 例：字段中文名="客户名称"，业务含义="客户名称" -> 有效
   - 例：字段中文名="开户日期"，业务含义="开户的日期" -> 有效
2. **有实质描述**：业务含义提供了字段中文名中没有的信息，比如业务背景、用途说明或具体描述
   - 例：字段中文名="客户状态"，业务含义="标识客户在系统中的当前状态，包括正常、冻结、注销等" -> 有效
   - 例：字段中文名="交易金额"，业务含义="记录一笔交易的实际发生金额，单位为元" -> 有效

### 判定为**无效**的情况：
1. **完全复制**：业务含义与字段中文名完全相同或几乎相同
   - 例：字段中文名="第二结论类型"，业务含义="第二结论类型" -> 第二结论不是常见名称，不属于见名知意，且业务含义未进行解释，无效
2. **语义重复**：业务含义与字段中文名在语义上完全等价，只是换了说法
   - 例：字段中文名="CDA规则结果"，业务含义="CDA的规则结果" -> 无效
   - 例：字段中文名="CDA规则结果"，业务含义="CDA规则结果字段" -> 无效
   - 例：字段中文名="转案欺诈类型"，业务含义="指转案欺诈的不同类别" -> 无效
3. **不相关**：业务含义和字段中文名没有联系，完全不相关

## 判断要求
- 仔细比较字段中文名和业务含义的语义差异
- 枚举值字段仅用于辅助理解字段语义，业务含义有效性的判定仍以"见名知意 + 是否有实质描述"为标准
- 对于模糊的情况，倾向于判定为有效
- 在reasoning字段中，请逐步写出完整的推理过程：先列出观察到的关键信息（字段中文名、业务含义），再说明判断依据（对照上述有效/无效标准逐条核验），最后解释你是如何权衡各种可能、得出最终结论的；reasoning字段请用中文描述，内容要完整详细
- 每行输入独立判断，不受其他输入内容影响

## 示例

输入：
[
  {"row_index": 0, "字段中文名": "客户名称", "业务含义": "客户名称"},
  {"row_index": 1, "字段中文名": "CDA规则结果", "业务含义": "CDA规则结果字段"}
  
]

输出：
[
  {
    "row_index": 0,
    "is_meaningful": true,
    "reasoning": "观察关键信息：字段中文名为'客户名称'，业务含义为'客户名称'，两者完全一致。对照判定标准：'见名知意'要求字段中文名本身足够清晰，'客户名称'是常见且语义明确的字段名，任何人一看即知其含义。业务含义虽未提供额外信息，但标准规定见名知意的字段无论业务含义如何写都视为有效。权衡：无模糊点，直接判定有效。"
  },
  {
    "row_index": 1,
    "is_meaningful": false,
    "reasoning": "观察关键信息：字段中文名为'CDA规则结果'，业务含义为'CDA规则结果字段'。对照判定标准：首先检查'见名知意'——'CDA规则结果'含缩写'CDA'，属于专有名称，字段名本身不足以让人理解，故不满足见名知意的豁免条件。其次检查业务含义是否提供实质信息：'CDA规则结果字段'仅是在原名后添加'字段'二字，属于'语义重复'情形，未解释'CDA'含义也未补充任何业务背景。权衡：既不满足见名知意，又仅是无意义加词，两者共同指向无效。"
  }
]"""

BUSINESS_MEANING_USER = """请对以下每一行数据判断"业务含义"是否有效。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""


# ============================================================
# 枚举值规范化 Prompt
# ============================================================

ENUM_NORMALIZATION_SYSTEM = """你的任务是将枚举值规范化为标准格式。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 枚举值: 原始枚举值字符串

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- has_codes: 布尔值，输入枚举值是否所有项都提供了代码。任一项缺少代码则为false
- needs_normalization: 布尔值，是否需要规范化（has_codes为true且格式与标准不一致时为true，否则为false）
- normalized: 规范化后的枚举值，格式为"代码-码值;代码-码值"，仅当needs_normalization为true时填写，否则为空字符串
- reasoning: 完整的逐步推理过程，必须写明判断依据、比较分析与权衡的逻辑

## 规则

1. **标准格式**：每项为"代码-码值"，代码与码值用英文连字符"-"连接，多项之间用英文分号";"分隔；码值文字原样保留，不做修改。
2. **代码**：只能是数字和字母的组合（如1、01、A、A01），保留原样——不补前导零，也不去掉已有的前导零。
3. **先判断 has_codes**：每一项必须同时有代码和码值才为true；只要有一项只有码值无代码（如"1-男;女"中的"女"），即为false。
4. **再判断 needs_normalization**（仅当 has_codes=true）：格式与标准不一致时为true，已是标准格式时为false。
5. **normalized 仅当 needs_normalization=true 时填写**：其他情况（has_codes=false 或已是标准格式）normalized 一律为空字符串。
6. **空值**：枚举值为空字符串时，has_codes=true、needs_normalization=false、normalized 为空字符串。
7. **去除干扰**：枚举值中可能包含额外的空格、冒号等，需要先去除。
8. **reasoning**：用中文描述判断依据。


## 格式对照（常见边界情况）

| 原始格式 | has_codes | needs_normalization | normalized | 说明 |
|---|---|---|---|---|
| 01-成功,02-失败 | true | true | 01-成功;02-失败 | 逗号改分号 |
| 01-成功;02-失败 | true | false | （空） | 已是标准格式 |
| 成功,失败,处理中 | false | false | （空） | 无代码 |
| (1)成功 (2)失败 | true | true | 1-成功;2-失败 | 去括号 |
| 01：成功 02：失败 | true | true | 01-成功;02-失败 | 中文冒号改连字符 |
| 01=成功\|02=失败 | true | true | 01-成功;02-失败 | 等号改连字符，\|明显是分隔符，改分号 |
| 男/女 | false | false | （空） | 斜杠分隔，无代码 |
| 账户冻结-银行发起-CLOP;可关闭状态-CLOR | true | true | CLOP-账户冻结-银行发起;CLOR-可关闭状态 | 代码在末尾，移到码值前 |
| 1-男;female-女 | true | false | （空） | 字母数字代码，已是标准格式 |
| 1-男;女 | false | false | （空） | "女"缺代码 |
| 成功,2-失败 | false | false | （空） | "成功"缺代码 |
| ：-1-初始化 1-审批中 2-审批通过 3-审批被拒 | true | true | -1-初始化;1-审批中;2-审批通过;3-审批被拒 | 冒号：是干扰噪音，去除后再规范化 |


## 示例

输入：
[
  {"row_index": 0, "枚举值": "1.现金 2.银行卡 3.支票"},
  {"row_index": 1, "枚举值": "01-成功;02-失败"},
  {"row_index": 2, "枚举值": "成功,失败,处理中"}
]

输出：
[
  {
    "row_index": 0,
    "has_codes": true,
    "needs_normalization": true,
    "normalized": "1-现金;2-银行卡;3-支票",
    "reasoning": "先判断has_codes：'1.现金 2.银行卡 3.支票'每项均有数字代码和码值，has_codes为true。再判断needs_normalization：当前用点号和空格分隔，与标准格式'代码-码值;代码-码值'不一致，需要规范化，将点号改连字符、空格改分号，代码原样保留，needs_normalization为true。最后给出normalized为'1-现金;2-银行卡;3-支票'。"
  },
  {
    "row_index": 1,
    "has_codes": true,
    "needs_normalization": false,
    "normalized": "",
    "reasoning": "先判断has_codes：'01-成功;02-失败'两项均为'代码-码值'，has_codes为true。再判断needs_normalization：分隔符为分号、代码与码值用连字符连接，与标准格式完全一致，不需要规范化，needs_normalization为false。因needs_normalization为false，normalized为空字符串。"
  },
  {
    "row_index": 2,
    "has_codes": false,
    "needs_normalization": false,
    "normalized": "",
    "reasoning": "先判断has_codes：'成功,失败,处理中'三项均只有码值、没有代码，has_codes为false。因has_codes为false，needs_normalization为false、normalized为空字符串。"
  }
]"""

ENUM_NORMALIZATION_USER = """请对以下每一行数据的"枚举值"进行规范化处理。

数据（JSON数组）：
{data}

请对每一行返回规范化结果，确保row_index与输入数据对应。"""


# ============================================================
# 字段所属类型检查 Prompt
# ============================================================

FIELD_TYPE_CHECK_SYSTEM = """你的任务是检查数据字段的"字段所属类型"是否填写正确。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 中文字段名: 字段的中文名称
- 业务定义: 字段的业务定义描述
- 字段所属类型: 字段所属类型

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- is_correct: 布尔值，字段所属类型是否填写正确（true=正确，false=可能错误）
- correct_type: is_correct为false时必填，填写正确的字段所属类型（六类之一），is_correct为true时为空字符串
- reasoning: 完整的逐步推理过程，必须写明判断依据、比较分析与权衡的逻辑

## 字段所属类型解释

- 文本类：适用于说明描述类信息，例如客户名称、电话号码等
- 数值类：适用于各类数量信息，如金额、价格、额度、人数、份数等
- 编码类：指数据被赋予具有一定规律，易于计算机和人识别处理的符号，适用于具有特定编码规则的数据项，例如证件号码、机构代码等
- 日期时间类：数据项存储的是日期、时间
- 标志类：数据项只有"是"和"否"两种取值
- 代码枚举类：适用于数据有明确分类、分组或多取值的数据项，比如类型、状态、种类、模式

## 判断要求

- 根据中文字段名和业务定义，判断该字段最可能应属于哪一类，再与申请单填写的字段所属类型比较
- 每条输入独立判断
- 判定为**可能错误**（is_correct=false）时，correct_type必填
- 判定为**正确**（is_correct=true）时，correct_type为空
- 在reasoning字段中，请逐步写出完整的推理过程：先列出字段名与业务定义中的关键信息，再逐一将六类类型的适用场景与当前字段对照，说明权衡后认为应属（或不应属）某类的依据，以及与申请单填写的类型比较后的结论；reasoning字段请用中文描述，内容要完整详细

## 示例

输入：
[
  {"row_index": 0, "中文字段名": "客户名称", "业务定义": "客户的姓名，用于标识客户身份", "字段所属类型": "文本类"},
  {"row_index": 1, "中文字段名": "交易金额", "业务定义": "记录一笔交易的实际发生金额，单位为元", "字段所属类型": "文本类"}
  {"row_index": 2, "中文字段名": "上传人", "业务定义": "分行邮寄单号导入文的上传人", "字段所属类型": "编码类"}
]

输出：
[
  {
    "row_index": 0,
    "is_correct": true,
    "correct_type": "",
    "reasoning": "关键信息：字段名'客户名称'，业务定义'客户的姓名，用于标识客户身份'。逐一对照六类：'客户姓名'是说明描述类信息，符合文本类'说明描述类信息'的适用场景；数值类需要数量语义，不符合；编码类需要特定编码规则，姓名无编码规则；日期时间类、标志类（仅是否）、代码枚举类（多取值分类）均不符合。权衡：文本类最贴切。申请单填写'文本类'，与实际判断一致。"
  },
  {
    "row_index": 1,
    "is_correct": false,
    "correct_type": "数值类",
    "reasoning": "关键信息：字段名'交易金额'，业务定义'记录一笔交易的实际发生金额，单位为元'。逐一对照六类：业务定义明确是'金额'且'单位为元'，金额属于数量信息，符合数值类'金额、价格'的适用场景；文本类用于说明描述，金额不是描述文本；编码类需要编码规则，金额无编码；日期时间类、标志类、代码枚举类均不匹配。权衡：'金额'这一数量属性明显指向数值类。申请单填写'文本类'，与应属类型不一致。"
  },
  {
    "row_index": 2,
    "is_correct": true,
    "correct_type": "",
    "reasoning": "关键信息：字段名'上传人'，业务定义'分行邮寄单号导入文的上传人'。逐一对照六类：业务定义没有说是人名还是人的编号，若理解为人名，符合文本类'说明描述类信息'的适用场景；数值类需要数量语义，不符合；若理解为人的编号，符合编码类；日期时间类、标志类、代码枚举类均不匹配。权衡：原始信息既可以理解为文本类，也可以理解为编码类。申请单填写'编码类'，与实际判断一致。"
  }
]"""

FIELD_TYPE_CHECK_USER = """请对以下每一行数据判断"字段所属类型"是否正确。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""


# ============================================================
# 枚举值语义关系判断 Prompt（软性检查前置判断，类型建议由代码根据当前类型生成）
# ============================================================

ENUM_ANTONYM_SYSTEM = """你的任务是判断两个词语是否构成"互补的二元对立"（非此即彼的二值状态）。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 字段中文名: 待检查字段的中文名称
- 词语1
- 词语2

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- is_antonym: 布尔值，两个词语是否构成互补的二元对立（true=构成，false=不构成）
- reasoning: 完整的逐步推理过程，必须写明判断依据、比较分析与权衡的逻辑

## 判断要求

- 直接比较"词语1"与"词语2"两个词语的语义
- **互补的二元对立**是指：两个词语语义上互为补集、互相排斥且穷尽该字段全部可能，
  表现为非此即彼的二值状态开关，例如：
  是/否、开启/关闭、启用/停用、通过/不通过、有效/无效、成功/失败、
  允许/禁止、受理/不受理、有/无、完成/未完成、成立/不成立 等
- 只有两个词语构成上述互补的二元对立时才判定为 true
- 明确**不构成**互补二元对立（判定为 false）的情况：
  - **存在中间态的相对反义**：大/小、高/低、冷/热、多/少 等（两者之间存在中间程度）
  - **方向/流程类的反向词**：流入/流出、收款/付款、借/贷、买入/卖出、转入/转出 等
    （互为对向关系，属于并列的方向/流程两端，而非状态性的非此即彼）
- 对于模糊、无法确定的情况，倾向于判定为 **true**
- 在reasoning字段中，请逐步写出完整的推理过程：先列出两个词语，再判断二者是否构成互补的二元对立（互斥、穷尽、无中间态），说明为何构成（或不构成）、遇到相对反义/方向反向词或模糊情况时为何倾向某种判定的权衡；reasoning字段请用中文描述，内容要完整详细

## 示例

输入：
[
  {"row_index": 0, "字段中文名": "审批结果", "词语1": "通过", "词语2": "不通过"},
  {"row_index": 1, "字段中文名": "资金流向", "词语1": "流入", "词语2": "流出"},
  {"row_index": 2, "字段中文名": "启用状态", "词语1": "启用", "词语2": "停用"},
  {"row_index": 3, "字段中文名": "收付款方向", "词语1": "收款", "词语2": "付款"}
]

输出：
[
  {
    "row_index": 0,
    "is_antonym": true,
    "reasoning": "两个词语为'通过'与'不通过'。对照判定标准：'通过'与'不通过'语义上互为补集、互相排斥且穷尽该字段全部可能，属于非此即彼的二值状态（是/否式互补二元对立）。权衡：无中间态、无模糊空间，判定为true。"
  },
  {
    "row_index": 1,
    "is_antonym": false,
    "reasoning": "两个词语为'流入'与'流出'。对照判定标准：二者是资金流动的两个方向，属于方向/流程类的反向词，互为对向关系而非状态性的非此即彼（资金还可处于不流动状态，并非穷尽全部可能）。权衡：属方向反向词，明确不构成互补二元对立，判定为false。"
  },
  {
    "row_index": 2,
    "is_antonym": true,
    "reasoning": "两个词语为'启用'与'停用'。对照判定标准：'启用'与'停用'语义完全对立，一方开启一方关闭，互斥且穷尽，属于典型的二值状态开关（与规则示例'启用/停用'一致）。权衡：互补二元对立明确成立，判定为true。"
  },
  {
    "row_index": 3,
    "is_antonym": false,
    "reasoning": "两个词语为'收款'与'付款'。对照判定标准：二者是资金收支的两个方向，属于方向/流程类的反向词，互为对向关系（一笔业务可同时涉及收款方和付款方），并非状态性的非此即彼。权衡：属方向反向词，不构成互补二元对立，判定为false。"
  }
]"""

ENUM_ANTONYM_USER = """请对以下每一行数据判断词语1与词语2是否构成互补的二元对立（非此即彼的二值状态）。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""


# ============================================================
# 数据示例语义检查 Prompt（软性检查）
# ============================================================

DATA_EXAMPLE_CHECK_SYSTEM = """你的任务是结合字段中文名、业务定义、字段所属类型，判断数据示例是否有效。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 数据示例
- 字段中文名
- 业务定义
- 字段所属类型

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- is_valid: 数据示例是否有效
- reasoning: 完整的逐步推理过程，必须写明判断依据、比较分析与权衡的逻辑

## 判断要求

1. 判断核心：本检查只关心"数据示例是否是字段应存类型/语义的代表"，**不关心示例值是否真实、常见或合理**。只要示例是字段所代表的同一类数据（人名字段给一个名字、时间字段给一个时间值、金额字段给一个数字），即使该值看起来特殊（如系统默认/起始日期 1899-12-31、哨兵数字等），也应判为有效。
2. 占位符判定仅限字面文本占位符：对于"NULL""空""无""测试数据""参考测试数据""待补充""TBD""N/A"等纯文本占位符，直接判定为无效；但不要因为某个值"看起来像默认/占位值"就判无效，只要它属于该字段的类型即可。
3. 如果数据示例本身有效，判断数据示例是否和字段中文名、业务定义以及字段所属类型的语义匹配：
   - 字段所属类型的解释
      文本类：适用于说明描述类信息，例如客户名称、电话号码等
      数值类：适用于各类数量信息，如金额、价格、额度、人数、份数等
      编码类：指数据被赋予具有一定规律，易于计算机和人识别处理的符号，适用于具有特定编码规则的数据项，例如证件号码、机构代码等
      日期时间类：数据示例是日期、时间
4. 每条输入独立判断
5. 在reasoning字段中，请逐步写出完整的推理过程：先判断数据示例是否为纯文本占位符，再结合字段中文名、业务定义与字段所属类型逐一核验语义是否匹配，说明判断依据与权衡；注意区分"示例不属于该字段类型"（无效）与"示例属于该字段类型但值特殊"（有效）。reasoning字段请用中文描述，内容要完整详细

## 示例

输入：
[
  {"row_index": 0, "数据示例": "张三，李四，王五", "字段中文名": "公司负责人", "业务定义": "公司负责人信息", "字段所属类型": "文本类"},
  {"row_index": 1, "数据示例": "7000001", "字段中文名": "上传人", "业务定义": "分行邮寄单号导入文的上传人", "字段所属类型": "编码类"},
  {"row_index": 2, "数据示例": "辽阳民兴支行", "字段中文名": "机构编号", "业务定义": "机构编号", "字段所属类型": "编码类"},
  {"row_index": 3, "数据示例": "个人金融业务部/综合管理部", "字段中文名": "维护人部门名称（一级部）", "业务定义": "角色数据权限维护功能维护这一条数据的维护人部门名称（一级部）", "字段所属类型": "文本类"},
  {"row_index": 4, "数据示例": "1899-12-31 11:00:38", "字段中文名": "系统批量加载数据时间", "业务定义": "系统批量加载数据的时间", "字段所属类型": "日期时间类"}
]

输出：
[
  {
    "row_index": 0,
    "is_valid": true,
    "reasoning": "先判断示例本身：'张三，李四，王五'是具体的人名列表，非占位符，本身有效。再核验语义：字段中文名'公司负责人'与业务定义'公司负责人信息'都指向人的信息，字段所属类型'文本类'用于说明描述类信息，人名列表符合文本类场景。权衡：示例与字段含义、类型均匹配，判定有效。"
  },
  {
    "row_index": 1,
    "is_valid": true,
    "reasoning": "先判断示例本身：'7000001'看起来是一串编号，非占位符，本身有效。再核验语义：字段中文名'上传人'与业务定义'分行邮寄单号导入文的上传人'都指向人，字段所属类型'编码类'指向编码，'7000001'如果指的是上传人的人工编号，整体就是合理的。权衡：示例与字段含义、类型均匹配，判定有效。"
  },
  {
    "row_index": 2,
    "is_valid": false,
    "reasoning": "先判断示例本身：'辽阳民兴支行'是具体内容，非占位符，本身有效。再核验语义：字段中文名'机构编号'与业务定义'机构编号'都表明该字段存储的是编码类数据项，字段所属类型为'编码类'；而示例'辽阳民兴支行'是一个机构名称，不是编号/编码形式。权衡：示例语义与字段应存储的编码内容明显不匹配，判定无效。"
  },
  {
    "row_index": 3,
    "is_valid": false,
    "reasoning": "先判断示例本身：'个人金融业务部/综合管理部'是具体内容，非占位符，本身有效。再核验语义：字段中文名'维护人部门名称（一级部）'要求的是单个一级部门名称，业务定义也强调'维护这一条数据的维护人部门名称（一级部）'；而示例包含两个部门名称并以'/'分隔，数量与粒度均与'一级部'的单数要求不符。权衡：示例内容超出字段定义范围，判定无效。"
  },
  {
    "row_index": 4,
    "is_valid": true,
    "reasoning": "先判断示例本身：'1899-12-31 11:00:38'是日期时间格式的具体值，不是NULL/空/测试数据等纯文本占位符。再核验语义：字段中文名'系统批量加载数据时间'与业务定义'系统批量加载数据的时间'都表明字段存储时间值，字段所属类型为'日期时间类'；示例'1899-12-31 11:00:38'正是一个日期时间值，属于该字段的类型与语义。虽然该时间点看起来像系统默认/起始日期，但检查只判断示例是否属于该字段的类型，不判断该具体值是否真实，故判定有效。"
  }
]"""

DATA_EXAMPLE_CHECK_USER = """请对以下每一行数据检查数据示例是否有效。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""
