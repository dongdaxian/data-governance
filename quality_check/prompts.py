"""LLM Prompt 模板。"""

# ============================================================
# 业务含义检查 Prompt
# ============================================================

BUSINESS_MEANING_SYSTEM = """你的任务是检查数据字段的"业务含义"描述是否有效。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 字段中文名: 待检查字段的中文名称
- 业务含义: 字段的业务含义描述

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- is_meaningful: 布尔值，业务含义是否有效（true=有效，false=无效）
- reason: 判断过程和原因说明，需详细描述比较分析的过程

## 判断标准

首先判断字段中文名是否有效。

### 判定为**有效**的情况：
1. **见名知意**：字段中文名本身足够清晰，业务含义无论怎样写都视为有效
   - 例：字段中文名="客户名称"，业务含义="客户名称" -> 有效
   - 例：字段中文名="开户日期"，业务含义="开户的日期" -> 有效
2. **有实质描述**：业务含义提供了字段中文名中没有的信息，比如业务背景、用途说明或具体描述
   - 例：字段中文名="客户状态"，业务含义="标识客户在系统中的当前状态，包括正常、冻结、注销等" -> 有效
   - 例：字段中文名="交易金额"，业务含义="记录一笔交易的实际发生金额，单位为元" -> 有效

### 判定为**无效**的情况：
1. **完全复制**：业务含义与字段中文名完全相同或几乎相同
   - 例：字段中文名="第二结论类型"，业务含义="第二结论类型" -> 第二结论不是常见金融名称，不属于见名知意，且业务含义未进行解释，无效
2. **简单加词**：业务含义只是在字段中文名基础上添加"的"、"是"、"字段"等无实质意义的词语
   - 例：字段中文名="CDA规则结果"，业务含义="CDA的规则结果" -> 无效
   - 例：字段中文名="CDA规则结果"，业务含义="CDA规则结果字段" -> 无效
3. **语义重复**：业务含义与字段中文名在语义上完全等价，只是换了说法
   - 例：字段中文名="转案欺诈类型"，业务含义="指转案欺诈的不同类别" -> 无效
4. **不相关**：业务含义和字段中文名没有联系，完全不相关

## 判断要求
- 仔细比较字段中文名和业务含义的语义差异
- 对于模糊的情况，倾向于判定为有效
- 在reason字段中，请详细说明你的判断过程，包括你如何比较两者、为什么认为有效或无效
- reason字段请用中文描述

## 示例

输入：
[
  {"row_index": 0, "字段中文名": "客户名称", "业务含义": "客户名称"},
  {"row_index": 1, "字段中文名": "CDA规则结果", "业务含义": "CDA规则结果字段"}
]

输出：
[
  {
    "row_index": 0,
    "is_meaningful": true,
    "reason": "字段中文名'客户名称'属于见名知意，业务含义即使与字段名相同也视为有效。"
  },
  {
    "row_index": 1,
    "is_meaningful": false,
    "reason": "字段中文名'CDA规则结果'不属于见名知意的常见金融名称，业务含义'CDA规则结果字段'只是在原名称基础上添加'字段'二字，未提供实质信息，无效。"
  }
]"""

BUSINESS_MEANING_USER = """请对以下每一行数据判断"业务含义"是否有效。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""


# ============================================================
# 枚举值规范化 Prompt
# ============================================================

ENUM_NORMALIZATION_SYSTEM = """你的任务是将枚举值规范化为标准格式，并判断枚举值是否都提供了代码。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 枚举值: 原始枚举值字符串

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- normalized: 规范化后的枚举值，格式为"代码-码值;代码-码值"
- needs_normalization: 布尔值，是否需要规范化（原始格式已是标准格式则为false）
- has_codes: 布尔值，输入枚举值是否所有项都提供了代码。任一项缺少代码则为false

## 标准格式要求

- 每个枚举项格式为"代码-码值"，代码与码值之间用英文连字符"-"连接
- 多个枚举项之间用英文分号";"分隔
- 代码只能是数字和字母的组合（如1、01、A、B、A01、B02等），保留原始代码原样：不补前导零，也不去掉已有的前导零
- 不要改变码值本身的文字内容

## has_codes 判断规则

- 枚举的每一项都必须同时提供代码和码值，has_codes才为true
- 只要有一项只有码值、没有代码（例如"1-男;女"中的"女"），has_codes就为false
- 常见有代码的形式：1-成功、01-成功、A-男、(1)成功、1.现金、账户冻结-银行发起-CLOP（代码CLOP在末尾）
- 常见无代码的形式：成功、男/女、成功,失败,处理中

## 标准格式示例
- 1-成功;2-失败;3-处理中（原始代码为1、2、3时）
- 01-成功;02-失败;03-处理中（原始代码本身是01、02、03时）
- A-男;B-女
- 1-现金;2-银行卡;3-支票
- A01-现金;B02-银行卡;C03-支票（字母与数字组合代码，原样保留）

## 常见不规范格式及处理方式

| 原始格式 | 规范化后 | has_codes | 说明 |
|---|---|---|---|
| 01-成功,02-失败 | 01-成功;02-失败 | true | 逗号改分号，代码原样保留 |
| 1.现金 2.银行卡 3.支票 4.汇款 | 1-现金;2-银行卡;3-支票;4-汇款 | true | 点号改连字符，代码原样保留（不补零） |
| 成功,失败,处理中 | 成功,失败,处理中 | false | 无代码，只提供码值，判为缺代码 |
| (1)成功 (2)失败 | 1-成功;2-失败 | true | 去除括号，代码原样保留 |
| 01：成功 02：失败 | 01-成功;02-失败 | true | 中文冒号改连字符，代码原样保留 |
| 01=成功\\|02=失败 | 01-成功;02-失败 | true | 等号改连字符，竖线改分号，代码原样保留 |
| 男/女 | 男/女 | false | 斜杠分隔，无代码，判为缺代码 |
| 账户冻结-银行发起-CLOP;可关闭状态-CLOR | CLOP-账户冻结-银行发起;CLOR-可关闭状态 | true | 代码和码值顺序反了，调整格式 |
| 1-男;female-女 | 1-男;female-女 | true | 每项都有代码，代码为数字和字母组合，无需修改 |
| 1-男;女 | 1-男;女 | false | "女"缺少代码，判为缺代码 |
| 成功,有-失败 | 成功,有-失败 | false | "成功"缺少代码，判为缺代码 |

## 处理要求
- 如果原始数据已经是标准格式（代码-码值;代码-码值），原样返回并设置needs_normalization为false
- 如果原始数据需要任何调整，设置needs_normalization为true并返回规范化后的结果
- 保留原始码值的文字内容，不要修改码值本身
- 如果has_codes为false，不做规范化：normalized原样返回原始枚举值，needs_normalization为false
- 如果枚举值为空字符串，原样返回空字符串并设置needs_normalization为false、has_codes为true

## 示例

输入：
[
  {"row_index": 0, "枚举值": "1.现金 2.银行卡 3.支票"},
  {"row_index": 1, "枚举值": "01-成功;02-失败"},
  {"row_index": 2, "枚举值": "成功,失败,处理中"}
]

输出：
[
  {
    "row_index": 0,
    "normalized": "1-现金;2-银行卡;3-支票",
    "needs_normalization": true,
    "has_codes": true
  },
  {
    "row_index": 1,
    "normalized": "01-成功;02-失败",
    "needs_normalization": false,
    "has_codes": true
  },
  {
    "row_index": 2,
    "normalized": "成功,失败,处理中",
    "needs_normalization": false,
    "has_codes": false
  }
]"""

ENUM_NORMALIZATION_USER = """请对以下每一行数据的"枚举值"进行规范化处理。

数据（JSON数组）：
{data}

请对每一行返回规范化结果，确保row_index与输入数据对应。"""


# ============================================================
# 字段所属类型检查 Prompt
# ============================================================

FIELD_TYPE_CHECK_SYSTEM = """你的任务是检查数据字段的"字段所属类型"是否填写正确。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 中文字段名: 字段的中文名称
- 业务定义: 字段的业务定义描述
- 字段所属类型: 字段所属类型

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- is_correct: 布尔值，字段所属类型是否填写正确（true=正确，false=可能错误）
- correct_type: 判断应为的字段所属类型（六类之一）。is_correct为false时必填
- reason: 判断过程和原因说明，需详细描述比较分析的过程

## 字段所属类型解释

- 代码枚举类：适用于数据有明确分类、分组或多取值的数据项，比如类型、状态、种类、模式
- 文本类：适用于说明描述类信息，例如客户名称、电话号码等
- 数值类：适用于各类数量信息，如金额、价格、额度、人数、份数等
- 编码类：指数据被赋予具有一定规律，易于计算机和人识别处理的符号，适用于具有特定编码规则的数据项，例如证件号码、机构代码等
- 标志类：数据项只有"是"和"否"两种取值
- 日期时间类：数据项存储的是日期、时间

## 判断要求

- 根据中文字段名和业务定义，判断该字段最可能应属于哪一类，再与申请单填写的字段所属类型比较
- 判定为**可能错误**（is_correct=false）时：
  - correct_type 必须是"数值类/编码类/代码枚举类/日期时间类/标志类/文本类"六类之一
  - reason 需具体说明为什么现有类型不符合、以及为什么应为该类型
- 判定为**正确**（is_correct=true）时，correct_type 可为空字符串
- reason字段请用中文描述

## 示例

输入：
[
  {"row_index": 0, "中文字段名": "客户名称", "业务定义": "客户的姓名，用于标识客户身份", "字段所属类型": "文本类"},
  {"row_index": 1, "中文字段名": "交易金额", "业务定义": "记录一笔交易的实际发生金额，单位为元", "字段所属类型": "文本类"}
]

输出：
[
  {
    "row_index": 0,
    "is_correct": true,
    "correct_type": "",
    "reason": "客户名称属于说明描述类信息，填写为文本类正确。"
  },
  {
    "row_index": 1,
    "is_correct": false,
    "correct_type": "数值类",
    "reason": "交易金额是金额类数量信息，应属于数值类而非文本类。"
  }
]"""

FIELD_TYPE_CHECK_USER = """请对以下每一行数据判断"字段所属类型"是否正确。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""


# ============================================================
# 枚举值语义关系判断 Prompt（软性检查前置判断，类型建议由代码根据当前类型生成）
# ============================================================

ENUM_ANTONYM_SYSTEM = """你的任务是判断枚举值的两项码值是否可理解为反义词或"是/否"关系。

## 背景

本判断只负责识别两项码值的语义关系；字段所属类型是否需要调整由后续业务规则
结合当前字段类型决定，输出中不需要推荐类型。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 字段中文名: 待检查字段的中文名称
- 枚举值: 该字段的枚举值，格式为"代码-码值;代码-码值"，含且仅含两项

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- is_antonym: 布尔值，两项码值是否构成反义词（true=反义词，false=不是）
- reason: 判断过程和原因说明，需详细描述比较分析的过程

## 判断要求

- **忽略枚举值中的代码前缀**，只比较"码值"部分的语义是否构成反义词
  - 例："01-通过;02-不通过" -> 比较"通过"与"不通过"，是反义词
  - 例："1-启用;0-停用" -> 比较"启用"与"停用"，是反义词
  - 例："1-成立;0-不成立" -> 比较"成立"与"不成立"，是反义词
- 反义词是指语义上互相矛盾、相对立的一对词，例如：
  通过/不通过、启用/停用、正常/异常、有效/无效、是/否、成功/失败、
  允许/禁止、受理/不受理、开放/封闭、有/无、完成/未完成 等
- 只有两项码值在语义上明确对立时才判定为反义词
- "借"与"贷"是会计记账方向，属于并列的业务方向，不属于"是/否"关系
- 对于模糊、无法确定的情况，倾向于判定为**不是反义词**（避免误报）
- reason字段请用中文描述

## 示例

输入：
[
  {"row_index": 0, "字段中文名": "审批结果", "枚举值": "01-通过;02-不通过"},
  {"row_index": 1, "字段中文名": "借贷方向", "枚举值": "1-借;2-贷"},
  {"row_index": 2, "字段中文名": "启用状态", "枚举值": "1-启用;0-停用"}
]

输出：
[
  {
    "row_index": 0,
    "is_antonym": true,
    "reason": "忽略代码后比较码值'通过'与'不通过'，两者语义完全对立，构成反义词。"
  },
  {
    "row_index": 1,
    "is_antonym": false,
    "reason": "忽略代码后比较码值'借'与'贷'，两者是会计记账方向，不是'是/否'或反义关系。"
  },
  {
    "row_index": 2,
    "is_antonym": true,
    "reason": "忽略代码后比较码值'启用'与'停用'，两者语义完全对立，构成反义词。"
  }
]"""

ENUM_ANTONYM_USER = """请对以下每一行数据判断枚举值的两项码值是否为反义词。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""


# ============================================================
# 数据示例语义检查 Prompt（软性检查：示例有效性 + 名称/类型一致性 + 关键数据项）
# ============================================================

DATA_EXAMPLE_CHECK_SYSTEM = """你的任务是结合字段中文名、业务定义、字段所属类型、域类型和数据示例，判断数据示例的业务语义风险。

## 输入数据结构

每行数据包含以下字段：
- row_index: 行号（输出时必须与此对应）
- 字段中文名
- 业务定义
- 字段所属类型
- 域类型
- 数据示例

## 输出字段结构

每行输出包含以下字段：
- row_index: 行号，与输入对应
- has_real_meaning: 数据示例是否有实际业务含义
- is_name_consistent: 数据示例是否与字段中文名语义一致；有歧义时填false
- is_type_consistent: 数据示例是否与字段所属类型语义一致；有歧义时填false
- suggested_field_type: 当is_type_consistent为false时，结合数据示例给出的疑似更合理类型；否则留空
- key_item_category: 无/机构/客户/员工/产品组/基础产品/可售产品/产品域层级
- key_item_needs_confirmation: 是否需要业务确认关键数据项的具体口径
- key_item_reason: 关键数据项口径研判过程和需要确认的具体问题；不需要时留空
- reason: 数据示例含义、名称一致性、类型一致性的判断过程和原因说明

## 判断要求

1. 先判断数据示例是否有实际业务含义：
   - "NULL""空""无""测试数据""参考测试数据""待补充""TBD""N/A"等疑似占位符，应判定为无实际业务含义；
   - 不能只做字面匹配，要结合字段语义判断示例是否只是测试值、示例说明、无意义符号或与业务无关内容；
   - 模糊、无法确定是否有业务含义时，倾向于判定为无实际业务含义。

2. 如果数据示例有实际业务含义，继续判断示例是否与字段中文名一致：
   - 不要求逐字相同，而是要求业务语义能够对应；
   - 如果字段名只能确定一个业务对象，而示例存在名称、编号、编码、列表、JSON等多种可能，应判定为不一致或存在歧义；
   - 例如"公司负责人"的示例是"张三，李四，王五"，无法确定是负责人名称列表、员工号列表还是其他列表，应提示确认。

3. 如果数据示例有实际业务含义，继续判断示例是否与字段所属类型一致：
   - 金额、数量、比例等示例与数值类更吻合；
   - 日期、时间、时间戳示例与日期时间类更吻合；
   - 有明确编码规则的标识与编码类更吻合；
   - 自然语言、长文本、JSON、列表等与文本类更吻合；
   - 只有"是/否"语义的示例与标志类更吻合；
   - 如果示例疑似某类型但当前类型不同，应在suggested_field_type中给出六类之一。

4. 重点识别机构、客户、员工、产品等关键数据项，并结合域类型和数据示例研判具体口径：
   - 机构：包括但不限于机构编号、机构号、机构代码、网点、分行、辖行。需要区分交行内部11位机构代码an!(11)、交行内部10位机构编号an!(10)，以及长度不定的外部机构号；
   - 客户：包括但不限于客户号、客户编号、ECIF客户号、总行客户号。多数应为16位ECIF客户号an!(16)，但也可能存在其他系统客户号且长度不定；
   - 员工：包括但不限于人工号、人编号、柜员号、柜员编号、员工号、经理编号。常见为EUIF的7位柜员/员工编号an!(7)；
   - 产品组：对应EPIM产品管理体系中的产品组标准编号an..(8)；
   - 基础产品：对应EPIM产品管理体系中的基础产品标准编号an..(13)；
   - 可售产品：对应EPIM产品管理体系中的可售产品标准编号an..(23)；
   - 产品域层级：对应EPIM产品管理体系中的产品域层级编号an..(2)。

5. Prompt中的例子只是说明判断方式，不是穷举。必须根据业务语义充分泛化，识别未列出的同类风险。

6. 不确定时倾向提示需人工确认，不要为了通过而强行判定一致。

## 示例

输入：
[
  {"row_index": 0, "字段中文名": "公司负责人", "业务定义": "公司负责人信息", "字段所属类型": "文本类", "域类型": "anc..(200)", "数据示例": "张三，李四，王五"},
  {"row_index": 1, "字段中文名": "机构编号", "业务定义": "机构编号", "字段所属类型": "编码类", "域类型": "an!(10)", "数据示例": "1234567890"},
  {"row_index": 2, "字段中文名": "备注", "业务定义": "备注信息", "字段所属类型": "编码类", "域类型": "anc..(20)", "数据示例": "客户申请已提交"}
]

输出：
[
  {
    "row_index": 0,
    "has_real_meaning": true,
    "is_name_consistent": false,
    "is_type_consistent": true,
    "suggested_field_type": "",
    "key_item_category": "无",
    "key_item_needs_confirmation": false,
    "key_item_reason": "",
    "reason": "示例为多个人员名称，但'公司负责人'无法确定是名称列表、员工号列表还是负责人清单，名称语义存在歧义。"
  },
  {
    "row_index": 1,
    "has_real_meaning": true,
    "is_name_consistent": true,
    "is_type_consistent": true,
    "suggested_field_type": "",
    "key_item_category": "机构",
    "key_item_needs_confirmation": true,
    "key_item_reason": "'机构编号'与10位域类型、10位示例吻合，但需要确认是交行内部10位机构编号，还是外部机构号或其他机构编码。",
    "reason": "示例为10位数字字符，与当前编码类和域类型语义一致。"
  },
  {
    "row_index": 2,
    "has_real_meaning": true,
    "is_name_consistent": true,
    "is_type_consistent": false,
    "suggested_field_type": "文本类",
    "key_item_category": "无",
    "key_item_needs_confirmation": false,
    "key_item_reason": "",
    "reason": "示例是自然语言描述，与文本类更吻合，与当前编码类不一致。"
  }
]"""

DATA_EXAMPLE_CHECK_USER = """请对以下每一行数据检查数据示例语义风险。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""
