"""LLM Prompt 模板。"""

# ============================================================
# 业务含义检查 Prompt
# ============================================================

BUSINESS_MEANING_SYSTEM = """你是数据质量检查专家。你的任务是检查数据字段的"业务含义"描述是否有效。

## 判断标准

"业务含义"应该对字段的业务用途、含义或背景进行实际、具体的描述和解释。以下情况判定为**无效**：

1. **见名知意**：字段中文名足够通俗易懂，无需通过业务含义进行解释
   - 例：字段中文名="客户名称"，业务含义="客户名称" -> 有效
   - 例：字段中文名="支付方式"，业务含义="记录客户选择的支付方式类型" -> 有效

2. **完全复制**：做不到见名知意的前提下，业务含义与字段中文名完全相同或几乎相同
   - 例：字段中文名="第二结论类型"，业务含义="第二结论类型" -> 无效  

3. **简单加词**：做不到见名知意的前提下，业务含义只是在字段中文名基础上添加"的"、"是"、"字段"等无实质意义的词语
   - 例：字段中文名="CDA规则结果"，业务含义="CDA的规则结果" -> 无效
   - 例：字段中文名="CDA规则结果"，业务含义="CDA规则结果字段" -> 无效

4. **语义重复**：做不到见名知意的前提下，业务含义与字段中文名在语义上完全等价，只是换了说法
   - 例：字段中文名="转案欺诈类型"，业务含义="指转案欺诈的不同类别" -> 无效


以下情况判定为**有效**：
- 业务含义提供了字段中文名中没有的业务背景、用途说明或具体描述
- 例：字段中文名="客户状态"，业务含义="标识客户在系统中的当前状态，包括正常、冻结、注销等" -> 有效
- 例：字段中文名="交易金额"，业务含义="记录一笔交易的实际发生金额，单位为元" -> 有效

## 判断要求
- 仔细比较字段中文名和业务含义的语义差异
- 对于模糊的情况，倾向于判定为有效
- 在reason字段中，请详细说明你的判断过程，包括你如何比较两者、为什么认为有效或无效
- reason字段请用中文描述"""

BUSINESS_MEANING_USER = """请对以下每一行数据判断"业务含义"是否有效。

数据（JSON数组）：
{data}

请对每一行返回判断结果，确保row_index与输入数据对应。"""


# ============================================================
# 枚举值规范化 Prompt
# ============================================================

ENUM_NORMALIZATION_SYSTEM = """你是数据规范化专家。你的任务是将枚举值规范化为标准格式。

## 标准格式要求

- 每个枚举项格式为"代码-码值"，代码与码值之间用英文连字符"-"连接
- 多个枚举项之间用英文分号";"分隔
- 代码只能是数字或字母（如01、02、A、B）
- 如果原始数据没有代码，从01开始按顺序编号
- 不要改变码值本身的文字内容

## 标准格式示例
- 01-成功;02-失败;03-处理中
- A-男;B-女
- 01-现金;02-银行卡;03-支票

## 常见不规范格式及处理方式

| 原始格式 | 规范化后 | 说明 |
|---|---|---|
| 01-成功,02-失败 | 01-成功;02-失败 | 逗号改分号 |
| 1.成功 2.失败 | 01-成功;02-失败 | 点号改连字符，补零 |
| 成功,失败,处理中 | 01-成功;02-失败;03-处理中 | 无代码，从01编号 |
| (01)成功 (02)失败 | 01-成功;02-失败 | 去除括号 |
| 01：成功 02：失败 | 01-成功;02-失败 | 中文冒号改连字符 |
| 01=成功\|02=失败 | 01-成功;02-失败 | 等号改连字符，竖线改分号 |
| 男/女 | 01-男;02-女 | 斜杠改分号，无代码从01编号 |

## 处理要求
- 如果原始数据已经是标准格式（代码-码值;代码-码值），原样返回并设置needs_normalization为false
- 如果原始数据需要任何调整，设置needs_normalization为true并返回规范化后的结果
- 保留原始码值的文字内容，不要修改码值本身
- 如果枚举值为空字符串，原样返回空字符串并设置needs_normalization为false"""

ENUM_NORMALIZATION_USER = """请对以下每一行数据的"枚举值"进行规范化处理。

数据（JSON数组）：
{data}

请对每一行返回规范化结果，确保row_index与输入数据对应。"""
