"""落标处理 LLM Prompt 模板。"""

# ============================================================
# 换域判断 Prompt
# ============================================================

DOMAIN_CHECK_SYSTEM = """为备选标准从可用域列表中挑选中文名与字典中文名不冲突的替代域。

你的任务：给定字典中文名和一组可用域列表，从中选择一个中文名与字典没有明显背离的域。

## 判断规则

1. 域的中文名与字典中文名不能有明显背离（大致符合即可，宽松检查）
   - 例如字典【基金账户余额】可以使用域【余额（元）】，但不能使用域【比率】
2. 如果可用域列表中有合适的域：needs_domain_change=true，并填写新域信息
3. 如果可用域列表中没有合适的域：needs_domain_change=false（该备选标准将被淘汰）

reason字段请用中文详细描述判断过程，包括各候选域中文名与字典中文名的匹配分析。"""

DOMAIN_CHECK_USER = """请对以下每条数据，从可用域列表中挑选中文名与字典不冲突的替代域。

数据（JSON数组，每项包含字典中文名和可用域列表）：
{data}

请对每条数据返回结果，确保row_index和candidate_index与输入数据对应。"""


# ============================================================
# 标准选择 Prompt
# ============================================================

STANDARD_SELECTION_SYSTEM = """从备选标准中为待落标字段选择最合适的标准。

## 选择结果分类

1. **复用已有标准**：某备选标准完全匹配待落标字段（名称、业务含义、域类型都匹配）
   - 结果为"复用已有标准"，记录选中的标准编号和标准名称

2. **复用已有标准但扩展业务定义**：某备选标准的业务定义相对于字段中文名过于狭窄，但扩展后可以匹配当前待落标字段
   - 结果为"复用已有标准但扩展业务定义"，记录选中的标准编号、标准名称和扩展建议
   - 扩展建议说明需要补充的业务定义内容

3. **新增标准**：没有合适的备选标准
   - 结果为"新增标准"，不填写标准编号和名称

## 判断要求

1. 仔细比较待落标字段的名称、业务含义、数据示例与每个备选标准的名称、业务定义、域类型
2. 优先选择名称和业务含义最匹配的备选标准
3. 如果备选标准的业务定义过于狭窄（如标准只覆盖部分场景），但扩展后可以匹配，选择"复用已有标准但扩展业务定义"
4. 如果没有备选标准能匹配，选择"新增标准"
5. reason字段请用中文详细描述比较分析的过程，包括每个备选标准的匹配度分析"""


STANDARD_SELECTION_USER = """请对以下每一行待落标字段，从其备选标准中选择最合适的结果。

数据（JSON数组，每项包含字段信息和备选标准列表）：
{data}

请对每一行返回选择结果，确保row_index与输入数据对应。"""
