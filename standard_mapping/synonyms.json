{
  "_comment": "近义词表：suffix_synonym_groups 为同义词组列表，每组是 2 个及以上同义后缀的数组，仅匹配字段名末尾位置，组内任意成员互为同义、双向替换。例：字段名以组内任一后缀结尾时，会用组内其他后缀各生成一个变体去召回；追加新组按同样格式新增数组即可。",
  "suffix_synonym_groups": [
    ["编号", "号"]
  ]
}
